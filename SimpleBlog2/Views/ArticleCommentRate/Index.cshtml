@model IEnumerable<SimpleBlog2.Models.ArticleCommentRateModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Oceny komentarzy do artykułó</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ArticleCommentRateId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArticleCommentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ArticleCommentRateId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ArticleCommentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.ActionLink("Edycja", "Edit", new { id=item.ArticleCommentRateId }) |
                @Html.ActionLink("Szczegóły", "Details", new { id=item.ArticleCommentRateId }) |
                @Html.ActionLink("Usuń", "Delete", new { id=item.ArticleCommentRateId })
            </td>
        </tr>
}
    </tbody>
</table>
